matrix:
  include:
    - language: node_js
      node_js:
        - stable
      cache:
        bundle: true
        directories:
          - node_modules
      before_script: cd frontend
      script:
        - yarn test
        - npm run build:dev
    # - language: python
    #   python: 3.6
    #   install:
    #     - pip install -r requirements.txt
    #   script:
    #     - pytest
